This folder is referenced by the database shell scripts when creating schema objects and loading them with data.

This folder contains the data files in zip format. Unzip the files before running the database shell scripts.

Example:

unzip large_data_set.zip
unzip master_cot_event.zip