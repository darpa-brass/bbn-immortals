buildscript {
    repositories {
        mavenCentral()
        jcenter()
        maven {
            url gradle.IMMORTALS_REPO
        }
        maven {
            url 'https://repo.gradle.org/gradle/libs-releases'
        }
    }
    dependencies {
        classpath 'mil.darpa.immortals:ImmortalsGradle:+'
        classpath 'mil.darpa.immortals:ImmortalsConfig:+'
        classpath 'com.android.tools.build:gradle:2.3.3'
    }
}

repositories {
    mavenCentral()
    maven {
        url gradle.IMMORTALS_REPO
    }
}

apply plugin: 'com.android.application'
apply plugin: 'mil.darpa.immortals'

group = 'mil.darpa.immortals'
version = '2.0-LOCAL'

android {
    compileSdkVersion 21
    buildToolsVersion "25.0.2"

    defaultConfig {
        applicationId "com.bbn.ataklite"
        minSdkVersion 21
        targetSdkVersion 21
        versionCode 1
        versionName version
    }

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
            assets.srcDirs = ['assets']
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    lintOptions {
        abortOnError false
    }

    preBuild.doLast {
        tasks.getByName('build').doLast {
            copy {
                from('build/outputs/apk') {
                    include '*.apk'
                }
                into('./')
            }
        }
    }

    clean.doLast {
        def f = new File('./*.apk')
        if (f.exists()) {
            f.delete()
        }
    }
}

// TODO: Matt wants these to be part of the ontology. Revisit once we have basic synthesis working...
dependencies {
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.google.android.gms:play-services-maps:8.3.0'
    compile 'com.android.support:support-v4:23.1.1'
    compile 'com.google.code.gson:gson:2.7'
    compile 'com.google.code.findbugs:jsr305:3.0.1'
    compile 'mil.darpa.immortals:core:+'
    compile 'com.securboration:immortals-adsl-generate:+'
    compile 'mil.darpa.immortals.datatypes:Coordinates:+'
    // KRHACK: The KR should not need these in the original project to perform adaptation!
    compile 'mil.darpa.immortals.dfus:JavaxCrypto:2.0-LOCAL'
    compile 'mil.darpa.immortals.dfus:BouncyCastleCipher:2.0-LOCAL'
    compile 'mil.darpa.immortals.dfus:BogoCipher:2.0-LOCAL'
    compile 'mil.darpa.immortals.dfus:NoOpCipher:2.0-LOCAL'
}
