package com.securboration.immortals.ontology.property.impact;

/**
 * Describes the expected but not necessarily observed impact of one thing on 
 * another (e.g., when memory footprint increases, so does bandwidth consumed)
 * 
 * @author jstaples
 *
 */
@com.securboration.immortals.ontology.annotations.RdfsComment(
    "Describes the expected but not necessarily observed impact of one" +
    " thing on  another (e.g., when memory footprint increases, so does" +
    " bandwidth consumed)  @author jstaples ")
public class PredictiveCauseEffectAssertion extends CauseEffectAssertion {
    

}
